
name: Deploy to GCP server

on: [workflow_dispatch]

jobs:
  build:
    runs-on: self-hosted
    
    steps:
      - uses: actions/checkout@v3
      
      - run: which fnm
      
      - run: which node
      
      - run: which npm
      
      - run: which pnpm

      - run: pm2 status
